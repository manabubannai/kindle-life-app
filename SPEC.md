# Kindle Life — SPEC

メルマガとブログを毎朝1冊のダイジェストにまとめてKindleに自動送信するツール。
非エンジニアが「Googleスプレッドシートをコピーするだけ」で使える形で配布する。

## 1. 背景と目的

- メルマガはGmailで埋もれ、ブログはブラウザで積読になる。Kindleなら「じっくり読む」体験に変えられる
- 既存の自作GAS 2ツール（auto-send-newsletter-to-kindle / auto-send-blog-to-kindle）で自分用には解決済み。これを統合し、非エンジニアの読者に届く形にするのが本プロジェクト
- ホスティング型のWebサービスは無料枠のメール送信上限（100〜500通/日）で収容人数が頭打ちになるため不採用。**各ユーザーのGoogleアカウント内で完結する「シートコピー方式」** を採る
  - 処理・送信枠・データがすべてユーザー側 → 人数無制限・運営コストゼロ円・プライバシー説明が最強（「あなたのデータは運営サーバーを一切通りません」）

## 2. ターゲットユーザー

- マナブの読者層（非エンジニア中心）。Googleアカウントを持ち、Kindle端末/アプリを使う人
- リリース判定基準: **非エンジニアがガイドだけを見て10分以内にセットアップ完了できる**こと

## 3. ユーザー体験

**シートは1タブのみ**。説明文と赤枠の入力エリア3つ（①Kindleアドレス ②メルマガ差出人リスト ③ブログURLリスト）だけで、説明書なしで使えることを狙う。配信時間帯・一時停止などのオプションは持たない（シンプルさ優先。配信は朝5時台固定、新着ゼロの日はスキップ固定）。

### 導入（1回だけ）
1. 配布ページの「コピーを作成」リンクをクリック
2. 赤枠①に自分の `@kindle.com` アドレス、②にメルマガの差出人アドレス、③にブログのURL（トップページでOK・フィード自動検出）を記入
3. カスタムメニュー「Kindle Life → 初期セットアップ」→ Google認証を1回許可
4. Amazon側で自分のGmailアドレスを承認済み送信者リストに追加（ガイドで案内）

### 日常（何もしない）
- 毎朝5時台に、前回以降の新着メルマガ + ブログ新着記事が **目次付き1冊のダイジェスト** としてKindleに届く
- 新着ゼロの日はスキップ

## 4. 機能要件

- **メルマガ収集**: ユーザーのGmailから登録差出人のメールを検索・取得（読み取りのみ）
- **ブログ収集**: RSS 2.0 + Atom のフィード巡回。抜粋フィードでも記事ページ本体から全文取得
- **フィード自動検出**: ユーザーはブログのトップページURLを貼るだけでよい（`<link rel="alternate">` → 定番パス試行の順で検出し、結果をキャッシュ）
- **ダイジェスト生成**: 目次（アンカーリンク）付き合本HTML。画像はbase64インライン埋め込み（上限 30枚/合計20MB/1枚5MB）
- **送信**: ユーザー自身のGmail送信枠から `@kindle.com` 宛にHTMLファイル添付で送信
- **重複防止**: 処理済みID/GUID管理（フィード別）。フィード追加時に過去記事が一斉配信されない
- **手動操作**: 今すぐ送信 / テスト送信 / 診断（メニューから）
- **エラー通知**: 失敗時は本人のGmailに平易な日本語メール（1日1通まで）+ 送信ログタブに記録
- **バージョン通知**: GitHubの version.json を1日1回照会し、重要更新時のみ本人に通知（同一版数につき1回）

## 5. 非目標（v1ではやらない）

- EPUB生成（HTML添付で十分な実績あり。v2の実験項目）
- ホスティング版・専用受信アドレス発行
- Gmail以外のメール受信箱対応
- リアルタイム個別送信（ダイジェストのみ。既存GAS 2ツールが個別送信の受け皿として併存）
- Chrome Web Store等のストア審査を伴う配布

## 6. 制約・クォータ検算（典型利用: メルマガ10誌+ブログ10フィード）

| クォータ | 上限（無料Googleアカウント） | 典型消費 |
|---|---|---|
| UrlFetchApp | 20,000回/日 | 50〜60回/日 |
| メール送信 | 100通/日 | 1〜2通/日 |
| 1実行時間 | 6分 | 2〜4分（4.5分ウォッチドッグで劣化送信に切替） |
| トリガー総実行 | 90分/日 | 5〜6分/日 |
| Gmail添付 | 25MB | 画像予算20MBで制御 |

唯一の本物の制約は「1実行6分」→ 時間ウォッチドッグで対処（DESIGN.md参照）。

## 7. 配布・収益

- GitHub公開（MIT）+ テンプレートシートの `/copy` リンク + ガイド（mblog.com 記事、日英）
- 収益モデルなし。既存ツール群と同じく mblog.com へのSEO集客・ブランド資産として機能させる（conventions.md の型）
